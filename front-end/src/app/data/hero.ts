/** Hero portrait, dropped in /public/hero/. */
export const HERO_PHOTO_SRC = '/hero/self_foto.png';

/** Fallback shape when the photo hasn't been provided. */
export const HERO_FRAME_SHAPE: 'circle' | 'rectangle' = 'rectangle';

/** Resume PDF, dropped in /public/. Opens in a new tab from the hero. */
export const HERO_RESUME_SRC = '/resume.pdf';
